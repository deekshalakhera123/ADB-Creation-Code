# ── PostgreSQL connection ─────────────────────────────────────────────────────

DB_CONFIG = {
    "host"    : "localhost",
    "port"    : 5432,          # int, not string
    "dbname"  : "newdatadb",   # Django's NAME → dbname
    "user"    : "postgres",    # Django's USER → user
    "password": "8171943405",
}