# ── PostgreSQL connection ─────────────────────────────────────────────────────

DB_HOST = "localhost"
DB_PORT = 5432              # int, not string
DB_NAME = "newdatadb"   # Django's NAME → dbname
DB_USER = "postgres"   # Django's USER → user
DB_PASSWORD= "8171943405"
